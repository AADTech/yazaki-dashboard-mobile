/*
(function () {
    'use strict';
    angular.module('MGMNTDASHBOARD.dashboard').directive('barGraph', function () {
            return {
                restrict: 'E',
                link: function (scope, element, attr) {
                    //element.append("<strong>" + attr.title + "</strong>");
                    console.log(attr.data);
                }
            };
        })
});
*/
